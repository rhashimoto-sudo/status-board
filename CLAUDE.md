# status-board（自分RPG ステータスボード）

## 思考原則

考えてから書け。最小限にしろ。余計なものに触るな。完了の定義を決めてから動け。
ただし強度はフェーズで変える: **設計・調査のとき**は複数案の比較・提案を妨げずに明らかに良い方法あればトレードオフを説明してから進める。

---

## 目的・解決したいこと

自分の成長を **9つのステータス（🧠INT / 💻TECH / 📊DATA / 📣MARKETING / 🌎ENGLISH / 👑PM / 🤝BRIDGE / 🔥LEARNING / ⚔️EXECUTION）× Lv × 称号** に落とし込み、九角形レーダーで「形」として可視化する個人用ダッシュボード。

解決したいのは以下:

- 成長がどの方向にどれだけ進んだか分からず、伸びている実感が持てない
- 「知った」（LEARNING）と「実際にやり切った」（EXECUTION）が混同されている → **2軸に分離して対比する**
- タスクの難易度・新規性が評価に反映されず、楽な作業と難しい挑戦が同じ「1件完了」に潰れる → **EXP = 難易度 × 新規性 × 完遂度 × 構造化ボーナス**
- 未達に実際のコストが無く継続が途切れる → **HP制ペナルティ + EXP減点**、HP 0 で GAME OVER（全ステータス初期化・殿堂に記録のみ永久保存）
- 弱い領域が得意領域の陰に隠れて放置される → **TOTAL Lv = 上位5平均×0.6 + 全9平均×0.4** で弱点を隠せなくする

**Step 1 の範囲**: ダミーデータで3タブ（ステータス / ミッション / 成長推移）のダッシュボードを完成させ Vercel に公開する。Notion API と GMエージェントには一切触らない。

---

## どこを読むか（ルーティング）

| 区分 | 読む場所 | 内容 |
|------|---------|------|
| 仕様書 | `docs/01_requirements.md` | 機能仕様・技術スタック・非機能要件（**正典**） |
| | `docs/02_architecture.md` | システム構成・ディレクトリ設計 |
| 本人 | `docs/00_profile.md` | 目的・3年逆算・今年の合格条件・9軸の重み・**燃料マップ**（運用エージェントは最初にこれを読む） |
| 運用 | `docs/13_deployment.md` | **本番URL**・Vercel の環境変数・動作確認の手順 |
| | `docs/10_notion_schema.md` | Notion スキーマ・work-dashboard との役割分担・9軸の対応表 |
| 進捗管理 | `docs/TODO.md` | セッション復帰用の一時ファイル（完了タスクは削除） |
| | `docs/WORK_LOG/` | 作業記録（日次） |
| | `docs/ERROR_LOG/` | エラー記録（日次） |

---

## ビルド・テスト・リント

```bash
npm run build
npm run dev
npm run lint
```

---

## プロジェクト固有ルール

- **調整値はすべて `src/lib/constants.ts` に一元管理する。** HP増減値・デバフ倍率・レベル曲線・EXP基礎値・スキル閾値をコンポーネントに直書きしない
- **ライトモードは実装しない。** `color-scheme: dark` 固定
- **アクセントはシアン `#22d3ee` × バイオレット `#8b5cf6` の2色のみ。** 状態色（HP の緑/黄/赤・緊急の赤・デバフの紫）だけが例外
- **発光はカウントダウンとHP危険域のみに限定する**
- **Lv・EXP・HP・カウントダウンは等幅フォント（`ui-monospace`）で桁を揃える**
- **3タブは完全分離。** 共通ヘッダー・固定の緊急バーは置かない
- **375px で横スクロールを発生させない**（レーダー・折れ線・ヒートマップ含む）
- **日々のEXP減点で Lv・称号は下がらない**（現Lvの下限累積EXPでフロアする）。下がるのは GAME OVER 時のみ
- **デバフは重複しない**（最も重い1つのみ適用）
- チャート実装に着手する前に配色・軸・凡例・ダークの規約を `dataviz` の規約に合わせる
- Step 1 は `src/data/*.json` の静的ダミーのみ。外部API・シークレットを持ち込まない
