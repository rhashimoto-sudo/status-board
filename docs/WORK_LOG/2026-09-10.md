# 作業ログ 2026-09-10

### 21:10 | docs/00_profile.md
**操作**: 修正
S-1（Lv1〜100 + levelCap への100段化。PR #4 で main にマージ済み）のあと docs 本体の改訂が
未了だったため、§8.3 の測定期間スコア算出式を旧スケール（Lv1〜5）から新スケール
（Lv10-50。`CALIBRATION_INITIAL_LEVEL_RANGE`）に合わせて改訂した。

### 21:10 | docs/01_requirements.md
**操作**: 修正
用語表・FR-1-2（称号）・FR-1-3（最終クラス）・FR-2（レベル曲線）・FR-2-2（levelCap。新設）・
FR-5-2（派生ツリー解放条件）・FR-6-2（EXPフロアの安全装置）・FR-7（測定期間の初期Lv算出式）・
FR-9（Step 1 ダミーデータの値）を、`src/lib/constants.ts` / `level.ts` / `skill.ts` の実測値に
合わせて改訂した。称号テーブル自体（`titles.ts`）は10段のまま変更されていないため、
「Lv10刻みの帯」という表現でその事実を正しく書いた。
また FR-9 の「派生は《データ分析》のみ解放済み」は、派生の解放条件が実効Lv50/70に
引き上げられた一方 `status.json` の実効Lvが最大41-42までしか届かないため事実と矛盾しており、
「現在すべて未解放」に訂正した（`src/data/status.json` は変更していない）。

### 21:10 | docs/03_status_system.md
**操作**: 修正
§2（称号テーブル）・§3.1（ステータスLv・levelCapの判定式）・§3.2（総合称号・最終クラス）・
§4（測定期間中の初期Lv範囲）・§5（ステータス一覧のASCII例・levelCap到達時の表示仕様を追記）・
§6（検証項目）を、実装（`level.ts`/`titles.ts`/`status-list.tsx`）に合わせて改訂した。
§5のASCII例は `src/data/status.json` の実測EXP値から実際に算出されるLv・称号に更新した。

### 21:10 | docs/04_exp_rules.md
**操作**: 修正
§4（レベル曲線）を100段の閾値抜粋 + levelCapの解説（§4.3として新設）に置き換え、
§5（TOTAL Lv）・§6（EXPフロア）・§8（検証項目）を実効Lv/生Lvの区別（`level.ts`/`exp.ts`の
実装、および `src/test/exp.test.ts` の不変条件）に合わせて改訂した。

### 21:10 | docs/07_unique_skill.md
**操作**: 修正
派生ツリーの解放条件（Lv5→Lv50、Lv7→Lv70）を `constants.ts` の `DERIVATIONS` に合わせて改訂し、
スキルツリーパネルの位置に関する記述（「紋章の直下」）を実際のTab1構成順
（`status-tab.tsx`。一覧・歪みメーターの後ろ）に合わせて訂正した。ASCII モックの派生欄も
`status.json` の実測値（現在すべて未解放）に更新した。

### 21:10 | docs/09_dashboard_spec.md
**操作**: 修正
`docs/TODO.md`「この実装で新たに生じた docs の追随」記載の3件を反映: (1) §2.1のASCII例の
EXP残量表記をTOTAL Lvの小数部表記に修正、(2) §2のTab1構成順を実装順
（一覧→歪み→固有スキル）に訂正、(3) §5（測定期間モード）に歪みメーターの非表示条件を追記。
あわせて §2.2 のレーダー軸最大値を「10固定」から「現在の levelCap に連動」（決定9 / 実装済み）
に、§4.1 の折れ線グラフY軸を `MAX_LEVEL`（100固定）に改訂した。

### 21:10 | docs/TODO.md
**操作**: 修正
「再開時の優先順」2.「docs本体の改訂」と「この実装で新たに生じたdocsの追随」3件を
完了扱い（チェック済み）にした。構造・他のネスト行は変更していない。
