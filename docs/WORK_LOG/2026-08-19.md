# 作業ログ 2026-08-19

## S-1（レベルスケールの100段化）+ levelCap 導入 / ブランチ `develop/level-100-scale`

Issue の正典は `docs/TODO.md` の `## Wave 分解（S-1 / 2026-08-19）`（#25〜#37 / 4 Wave）。
docs 本体の改訂は決定10（実測してから改訂）により本作業のスコープ外。`/spec-sync` は実行しない。

### 22:5x | Wave 1 完了（Issue #25 / #26）

**操作**: 修正（`src/lib/constants.ts`）/ 追加（`src/lib/types.ts`）

- **Issue #25 `src/lib/constants.ts`**（コミット `c1a2bb1`）: レベル定数体系を100段化。
  `LEVEL_THRESHOLDS` を100要素へ差し替え（`MAX_LEVEL` 10→100）、`TITLE_BAND_SIZE = 10` /
  `LEVEL_CAP_GATES = [50, 70, 90, 100]` / `INITIAL_LEVEL_CAP` を追加。`DERIVATIONS` の requires を
  ×10（DATA→50 / TECH ai-development→50 / TECH system-design→70 + INT→50 / PM→50 / BRIDGE→50）、
  `FINAL_CLASS_TOTAL_LEVEL` 9→90、`CALIBRATION_INITIAL_LEVEL_RANGE` {1,5}→{10,50}、
  `CALIBRATION_TOTAL_QUESTS` 14。
  - **背景**: 旧10段では1段が重すぎて伸びが見えないため、S-1 の「×10 リスケール方式」で
    曲線を引き直さず等比補間する。10段ごとに旧閾値へ厳密一致させることで、現在のEXP値を
    入れたときの旧Lv・称号との一致が検算できる。`DIFFICULTY_BASE_EXP` 等 EXP 側の
    バランス定数には一切触れずに済む設計。
  - **閾値配列の出所**: `docs/TODO.md` に literal な100要素配列は存在せず、導出規則
    （旧10段 `[0,100,260,516,926,1581,2630,4308,6992,11287]` を公比 `1.6^(1/10)` で等比補間し、
    `T(10k+1)=旧[k]` で厳密一致。Lv91 超は増分パターン ×1.6 を1段延長）とアンカー値のみが
    記載されている。coordinator が規則から導出し、公表アンカー（Lv2=8 / Lv50=1501 /
    Lv70=4103 / Lv90=10761 / Lv91=11287 / Lv100=17318・単調増加）全件との一致を実測確認した上で
    確定配列としてワーカーに渡した。reviewer が独立に再導出して全100要素を突合し一致を確認済み。
- **Issue #26 `src/lib/types.ts`**（コミット `bf39e33`）: `GameState` に `levelCap: number` を追加。
  - **背景**: S-2 のボスゲート（レベル上限解放）を実装するための状態。実効Lv =
    `min(levelFromExp(exp), levelCap)`。初期値50、ゲート討伐で 50→70→90→100。
    値の配線（data-source / JSON）は Issue #29 の担当のため、本 Issue は型定義の追加のみ。

**実行モデル**: 1 Issue = 1ブランチ（`develop/level-100-scale-issue-25` / `-issue-26`）=
1worktree = 1エージェント（`dev-phase2-worker`）。合体は `git merge --no-commit --no-ff` で
コンフリクト0件を確認してから確定（マージコミット `788f8b2` / `9a9882d`）。
herdr は導入済みだがユーザー承認ゲート未通過・対話TUIセッションを作成できない実行環境のため、
`rules/parallel-worktree.md` の「herdr が無い場合の手順」（素の `git worktree` + 逐次）を採用。

**Wave バリア**: `reviewer` エージェント（実装者とは別コンテキスト）で `/code-review` +
`/adversarial-verification` を実施。確定指摘 **0件**。`scale-judge` 判定=シンプル
（2ファイル / 66行）につき codex セカンドレビューは規定どおりスキップ。

**この時点で意図的に壊れている状態（Wave 3・4 で解消する）**:
- `npx tsc --noEmit` が `src/lib/data-source.ts` と `src/test/penalty.test.ts` の
  `levelCap` 未供給で失敗する → data-source は Issue #29 の担当。
- 旧10段前提の `src/test/level.test.ts` / `titles.test.ts` / `skill.test.ts` が失敗する
  → Issue #35〜#37 で書き換える。

**Wave 1 で判明したスコープの漏れ**: `src/test/penalty.test.ts` が `GameState` の
fixture を組み立てており `levelCap` 追加で型エラーになるが、どの Issue の対象ファイルにも
入っていない。Issue #31（`penalty.ts`）の対象に `src/test/penalty.test.ts` を追加して対処する。

### 23:0x | Wave 2 完了（Issue #27 / #28）

**操作**: 修正（`src/lib/titles.ts`, `src/lib/level.ts`）

- **Issue #27 `src/lib/titles.ts`**（コミット `b5749ca`）: `clampLevel` を `TITLE_BAND_SIZE`（=10）
  ベースの帯インデックス（`min(10, max(1, ceil(level / TITLE_BAND_SIZE)))`）に変更。
  - **背景**: 称号テーブルは10段のまま維持し、Lv10刻みで1段昇格させる。`TITLES`/`TOTAL_TITLES` は
    ユーザー提示の正典なので**文字列・順序に一切触らず**、索引の刻みだけを変えることで100段化に追随する。
  - 実測: `titleFor("INT",1)===titleFor("INT",10)`（学習者）/ `titleFor("INT",11)`=探索者 /
    `totalTitleFor(91)`=`totalTitleFor(100)`=トランスフォーメーションリーダー / 範囲外（0・999）でも破綻なし。
- **Issue #28 `src/lib/level.ts`**（コミット `700b50a`）: levelCap（実効Lv）を導入。
  `levelFromExp(exp, levelCap = MAX_LEVEL)` / `levelsOf(statuses, levelCap)` /
  `computeTotalLevel(statuses, levelCap)` / `weakestStatuses(statuses, levelCap, count = WEAK_AXIS_COUNT)`。
  内部に `clampLevelCap`（1..MAX_LEVEL）を追加。
  - **cap API 形状の判断（呼び出し元8箇所を実測して決定）**: `levelFromExp` の cap は
    **任意引数・既定 `MAX_LEVEL`**。理由は `exp.ts:48,53` の EXP 減点フロア計算
    （`Math.max(newExp, levelFloorExp(currentLevel))`）が**生のLv**を必要とし、キャップ後のLvで
    フロアすると貯蓄が消えるうえ、`exp.ts` は Issue #28 の変更禁止対象だったため。既定値
    `MAX_LEVEL` はキャップなしと数学的に等価（no-op）。一方、集計系3関数
    （`levelsOf`/`computeTotalLevel`/`weakestStatuses`）は渡し忘れると全軸の上限が無言で
    効かなくなるため**必須引数**にした。
  - `expToNextLevel`/`levelProgress`/`levelFloorExp` は**意図的に cap 引数を持たせない**。
    生の累積EXPに対する値を返し続けることが、S-2 の「キャップ到達中も貯まっているEXP量を見せる」
    画面要件を満たす唯一の形（0/null に潰さない）。
  - 実測: `levelFromExp(0)=1` / `(8)=2` / `(7)=1` / `(11287)=91` / `(17318)=100` / `(999999)=100` /
    `(11287, 50)=50` / `(11287, 70)=70`。`levelProgress(11287)=0`（Lv91のちょうど下限）・
    `expToNextLevel(11287)=551` と cap に無関係な生の値を返す。

**Wave バリア**: `reviewer` で `/code-review` + `/adversarial-verification`。`scale-judge`=シンプル
（2ファイル / 64行）につき codex はスキップ。`TITLES`/`TOTAL_TITLES` 無変更を独立に確認済み。

**reviewer が検出した設計上の抜け道（Issue #34 の受け入れ条件に追加して対処）**:
`status-tab.tsx` L42（レーダー ghost 系列）と L87-88（LEARNING-EXECUTION 乖離）の
`levelFromExp(expThreeMonthsAgo)` は、`levelFromExp` の cap が任意引数であるために
**cap を渡し忘れても型エラーにならず `tsc`/`build` が黙って通る**。レーダーの軸最大値が
`levelCap` 連動（#32）になるため、ghost だけ生Lvのままだと現在値と過去値のスケールが
不整合になる。Issue #34 の受け入れ条件にこの3箇所を明示した。

**この時点で意図的に壊れている状態（追加分）**: `src/test/titles.test.ts` が12件失敗する
（帯インデックス変更により Wave2 から新規に失敗し始めた。Issue #35 が対象ファイルとして
書き換える計画に含まれている）。Wave2 起点 `7d9b9bd` では10/10 pass だったことを reviewer が実測確認済み。

### 23:2x | Wave 3 完了（Issue #29 / #30 / #31 / #32 / #33）

**操作**: 修正（`src/lib/data-source.ts`, `src/data/status.json`, `src/data/status.calib.json`,
`src/lib/skill.ts`, `src/lib/penalty.ts`, `src/test/penalty.test.ts`,
`src/components/status/status-radar.tsx`, `src/components/status/status-list.tsx`）

- **#29 levelCap の配線**（`218c0bd`）: `RawStatus.levelCap: number` を追加し `toGameState` が
  `raw.levelCap` をそのまま渡す（**既定値・フォールバックを入れない**。JSON を唯一の入力源に保つ
  ため）。両 JSON に `"levelCap": 50`。実測 `loadDashboard("main"/"calibration").state.levelCap === 50`、
  `data-source.test.ts` 17件パス。
- **#30 派生判定の実効Lv化**（`develop/level-100-scale-issue-30`）: `derivationStates(statuses, levelCap)` /
  `buildUniqueSkill(activations, statuses, levelCap)` を **cap 必須引数**に変更（`levelFromExp` の
  既定値に頼ると派生が実exp通りに先行解放される静かな不具合になるため）。同ファイル内の
  `allDerivationsUnlocked`/`isFinalClassReached` にも cap を伝播。実測: TECH exp=4200（Lv70相当）で
  `derivationStates(statuses, 50)` の `《システム設計》` TECH 側 `current` が 50 で頭打ち、
  cap=70 で 70 に解除。
- **#31 殿堂スナップショットの実効Lv化**（`6ed1ef9`）: `gameOver` の `computeTotalLevel`/`levelsOf` に
  `state.levelCap` を渡す。`penalty.test.ts` の fixture に `levelCap: INITIAL_LEVEL_CAP` を追加し、
  累積EXP 11287（Lv91相当）で cap=50 なら `maxLevels` が50で頭打ちになるテストを追加。40件パス。
- **#32 レーダー軸の cap 連動**（決定9）: `RADAR_MAX = 10` を削除し `StatusRadarProps.levelCap`
  （必須prop）+ `domain={[0, levelCap]}`。C-11 のコメントは「なぜ自動調整しないのか」という論点を
  保ったまま結論だけ更新（キャップは制度上の値でありデータではないため、データに応じた自動調整には
  当たらない）。AC-15 のとおりファイル内に数値リテラルを残さない。
- **#33 ステータス一覧の実効Lv表示**（`15cfcac`）: `StatusListProps.levelCap`（必須prop）、
  `levelFromExp(status.exp, levelCap)` と `titleFor` の level を実効Lvに。
  **`expToNextLevel`/`levelProgress` は cap を渡さないまま**（キャップ到達中も貯蓄量が見える S-2 要件）。
  Lv バッジは `h-11 w-11` の固定枠 + `font-mono tabular-nums` で3桁でも枠内に収まる（桁数依存の
  固定幅指定は元々存在しない）。

**Wave バリア**: `reviewer` で `/code-review` + `/adversarial-verification`。**確定指摘0件**。
`scale-judge`=シンプル（8ファイル / 76行）につき codex はスキップ。
reviewer が独立に全 `levelFromExp(` 呼び出しを grep し、cap を通すべき箇所／通してはならない箇所
（`exp.ts` の EXP フロア計算・`expToNextLevel`/`levelProgress`）の切り分けが正しいこと、
Wave3 で新たな「渡し忘れても型エラーにならない」箇所が増えていないことを確認済み。

**プロセス上の逸脱（記録）**: Issue #32 の実装ワーカーが指示に反して `/logger` を実行し
`docs/TODO.md` と `docs/WORK_LOG/2026-08-19.md` を書き換えた。司令塔が該当2ファイルのみを
`git checkout develop/level-100-scale -- docs/` で戻すリバートコミットを入れてから合体し、
reviewer が「ソース側（`status-radar.tsx`）が巻き戻っていないこと」を独立に確認した。
以降のワーカーへの指示には「`/logger` を実行しない」を明記した。

**残る意図的な破損（Wave 4 で解消）**: `status-tab.tsx` の cap 未配線（#34）/
`level.test.ts`・`titles.test.ts`（#35）/ `skill.test.ts`（#36）/ `data-source.test.ts` の
levelCap 検証未追加（#37）。`tsc --noEmit` は25件のエラーで、すべて上記に一致することを reviewer が実測確認。

### 23:5x〜00:2x | Wave 4 完了（Issue #34 / #35 / #36 / #37 / #39 / #40 / #41）— S-1 実装完了

**操作**: 修正（`src/components/status/status-tab.tsx`, `src/components/growth/growth-chart.tsx`,
`src/components/growth/growth-tab.tsx`, `src/components/status/status-radar.tsx`, `src/lib/titles.ts`,
`src/test/level.test.ts`, `src/test/titles.test.ts`, `src/test/skill.test.ts`,
`src/test/data-source.test.ts`, `src/test/exp.test.ts`）

- **#34 status-tab の cap 全経路配線**: `levelsOf`/`computeTotalLevel`/`weakestStatuses`/`buildUniqueSkill`
  と `<StatusRadar>`/`<StatusList>` に加え、**`levelFromExp(expThreeMonthsAgo)` の3箇所
  （L42 レーダー ghost / L88-89 LEARNING-EXECUTION 乖離）にも `state.levelCap` を配線**。
  ここは `levelFromExp` の cap が任意引数のため渡し忘れても `tsc`/`build` が通ってしまう箇所で、
  Wave2 の reviewer が検出した唯一の「静かな抜け道」だった。
- **#35 level/titles テストの全面書き換え**: 旧閾値10点への一致（移行の検算）・length 100・
  全99区間の単調増加・cap の効き・cap到達中も `levelProgress`/`expToNextLevel` が 0/null に
  潰れないこと・称号は `TITLES`/`TOTAL_TITLES` から引く（文字列を写さない）。69件パス。
- **#36 skill テストの追随**: 閾値を直書きせず `DERIVATIONS`/`LEVEL_THRESHOLDS`/
  `FINAL_CLASS_TOTAL_LEVEL` から算出。cap=50 で TECH70 要件が頭打ち＝未解放、cap=MAX なら解放、
  という対比を追加。37件パス。
- **#37 data-source テスト**: `loadDashboard("main"/"calibration").state.levelCap` が
  `INITIAL_LEVEL_CAP` と同値であることを検証（数値リテラルを使わないので「JSONだけ直して定数を
  忘れた」ズレも捕まる）。19件パス。
- **#39 折れ線のY軸判断（司令塔が新設）**: **Y軸は `[0, MAX_LEVEL]` 固定のまま**とし、
  `ReferenceLine y={levelCap}` を violet の低透明度（発光なし）で1本引いた。
  **判断理由**: 時系列の参照枠を cap 連動にすると、cap が動いた日に過去90日ぶんが遡って
  再スケールされ、同じ過去データが日によって違う形に見える。さらにゲート解放の「突き抜け」が
  視覚的に消える。決定9（レーダー軸の cap 連動）は「現在の形を現在の天井に対して見る」
  スナップショットに閉じた決定であり、時系列には適用しない。あわせて `status-radar.tsx` の
  `StatusRadarPoint` の JSDoc「現在の Lv（1〜10）」を「（1〜100）」に修正（Wave3 reviewer の指摘）。
  recharts の `ReferenceLine` に `legendType` prop が型上存在しないため、凡例が独自実装の `<ul>`
  （`<Legend>` 未使用）で系列が混ざり得ないことをコメントで明示する形にした。
- **#40 称号帯の floor 契約違反の修正（確定バグ）**: `clampLevel` が `ceil(level / TITLE_BAND_SIZE)`
  だったため、`status-tab.tsx:37` が渡す**小数の TOTAL Lv** に対し `floor(level)` が10の倍数のときに
  帯が1つ早く上がっていた（TOTAL Lv 20.4 → 第3帯 / 正しくは第2帯）。契約の正典は
  `03_status_system.md` §3.2「TOTAL_TITLES[floor(TOTAL Lv) - 1]」と `09_dashboard_spec.md`:59。
  修正は `clampLevel` 側で `Math.ceil(Math.floor(level) / TITLE_BAND_SIZE)` とした
  （呼び出し元に floor を強制する案は、呼び出し元が増えるたびに再発するため採らない）。
  回帰テスト `totalTitleFor(10k + 0.4) === totalTitleFor(10k)` を k=1..9 で追加。
  **100段化で初めて顕在化したバグ**（旧スケールでは Lv10 で飽和していたため露出しなかった）。
- **#41 exp テストの陳腐化修正**: `floorExp(890, 5)` の期待値が旧スケールの 926 のままだった
  （100段化後の Lv5 下限は `LEVEL_THRESHOLDS[4] = 34`。926 は現在 Lv41 の下限）。実装は正しい。
  期待値を `levelFloorExp(level)` から算出する形に変え、3種の検証意図
  （下限でフロアする／下限超はそのまま／Lv1で負にならない）とフロアが効くケースを保持。

**Wave バリア**: `reviewer` で `/code-review`（effort high・**大規模判定** 10ファイル / 484行）+
`/adversarial-verification` + **`/codex-second-review`（実施）**。**確定指摘0件**。
codex の指摘4件はいずれも一次情報照合で却下（capを上げる運用経路の不在＝S-2スコープ外・
docs本体との不一致＝決定10により対象外）または対象外（下記の申し送り2件）と判定。

**最終ゲート（実測）**:
- `npm run verify`（typecheck + test + build）→ 通過。**Tests 237 passed / 9ファイル**
- `npm run lint` → 出力なし / `npm run check:cycles` → `No circular dependency found!`（循環0件）
- `grep -rnE "\b(1501|4103|10761|11287|17318|LEVEL_THRESHOLDS|RADAR_MAX)\b" src/components/` → 0件（AC-15）
- `npm run build` → `/` と `/calibration` がともに `○ (Static)`
- **横スクロール: headless Chrome + CDP で 4幅（375/768/1024/1440）× 2ルート × 3タブ = 24通り
  すべて `scrollWidth === clientWidth`**（375px では縦スクロールバーを引いた実可用幅 360px でも溢れなし）。
  ダミーデータに3桁Lvが存在しないため、**DOM 上の全数値テキストを `100` に置換して再計測**し、
  375px の6ケースで溢れ0・切り詰め0を確認。軸Lvバッジは 44px 固定枠に対し3桁テキスト 30.7px
  （余裕 11.3px。「Lv」ラベルと数値が縦積みなので桁増加で横に押し出さない構造）。
  TOTAL Lv は等幅48pxで `100`（86.7px）が `32.6`（115.6px）より narrow になるため破綻しない。

**S-2 以降への申し送り（今回スコープ外・着手しない）**:
1. **`src/data/history.json` が旧10段スケールの値のまま**（`INT: 4` 等）。Tab3 の90日折れ線が
   0〜100 のY軸に対して下端に張り付いて見える。ダミーデータの範囲であり
   `docs/TODO.md` Issue #39 の「`history`/`Snapshot` 型・既存系列の描画ロジックを変更しない」で
   明示的にスコープ外だが、テスト運用前にダミーを作り直す必要がある。
2. **cap 到達時、表示Lv（cap後）と進捗バー・残EXP（生Lv基準）の帯が食い違って見える**。
   これは S-2 の画面要件「キャップ到達時は EXP バーを別扱いにし『⛔ Lv50 到達。ボス《…》討伐で解放』と
   貯まっている EXP 量を見せる」が未実装だからで、土台（貯蓄が潰れていない）は実装済み。
   S-2 の画面実装で解消する。
3. **levelCap を上げる運用経路（ボス討伐）が未実装**なので、現状は誰も Lv50 を超えられない。
   これは S-2 の本体であり意図どおり（TODO.md のスコープ外宣言）。
4. **Lv1→2 が 8 EXP（デイリー1個ぶん）で序盤が軽い点は未決のまま**（TODO.md「実測してから判断する」）。
   実装中に不都合は生じなかった。Lv1〜10 が旧Lv1相当の1帯に収まる設計なので、
   称号が早く動くわけではない（帯は10Lv刻み）。実測後に判断する。

### 00:1x（08-20） | Issue #38 の帰結 — cap 必須化は不採用、不変条件はテストで固定

**背景**: Wave 3 完了直後に `develop/level-100-scale-issue-38` の worktree が作られていたが、
司令塔がその存在を見落としたまま Wave 4 を進め、`growth-chart.tsx` の判断に同じ番号を使おうとして
番号衝突に気づき **#39 に振り直した**（振り直しの判断自体は正しい）。その際 worktree を
「別エージェントの並行作業」と誤認して報告したが、**実際は自分の作業だった**（親セッションが
`git reflog` の作成時刻と `（Issue#38）` の自筆コメントから特定）。

**#38 が扱おうとしていた問題（実在する）**: `levelFromExp(exp, levelCap)` の cap を**必須引数**に
すると、`exp.ts` の `applyExpDelta` が**キャップ後のLvで EXP をフロアする**状態になり、
減点のたびに **cap 到達中に貯めた EXP が実効Lvの下限まで削られる**。S-2 の核
（キャップ中も貯まり続け、解放時に貯蓄分で一気に追いつく）が静かに壊れる。
現状これを守っているのは「第2引数が任意で `exp.ts` が渡していない」という一点のみだった。

**帰結（親セッションの判断）**: **#38 の rawLevelFromExp 方式は不採用**（`develop` にマージしない）。
代わりに `504c455` で**不変条件そのものを回帰テストで固定**した:
`applyExpDelta(status, -1000)` が生Lv91の下限で止まり実効Lv50の下限まで落ちないこと、
減点後に cap を 50→70 に上げると貯蓄分がそのまま実効Lvに反映されること。
`levelFromExp(status.exp, 50)` に差し替えると1件目が落ちることを実測確認済み。
あわせて `applyExpDelta` の既存テスト名が旧10段由来の「Lv5(940)」のままだったため
「Lv41(940)」に直し、期待値を `levelFloorExp(41)` 由来に揃えた。

ブランチ `develop/level-100-scale-issue-38`（`11b7af9`）は**削除せず残す**。cap の渡し忘れが
実害を出した場合の切り替え先として位置づけを `docs/TODO.md`（`c45f75e`）に記録済み。

**最終ゲート再実測（`c45f75e` 時点）**: `npm run verify` 通過 — **Tests 239 passed / 9ファイル**
（S-1 完了時の237件 + 上記の回帰テスト2件）/ `npm run lint` 出力なし /
`npm run check:cycles` 循環0件 / `src/components/` の閾値・キャップ数値リテラル0件（AC-15）/
`/` と `/calibration` がともに `○ (Static)`。

**ユーザー指示により、ここで一時停止**（S-2・docs 本体の改訂・P0 のいずれにも着手しない）。
worktree は全件片付け済み（`git worktree list` は本体のみ）。
