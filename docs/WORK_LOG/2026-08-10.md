# 作業ログ 2026-08-10

Wave 4（Issue #8 / #9 / #10）の実装と Wave バリア（`/code-review` → Codex セカンドレビュー →
指摘修正 → 再マージ）を実施した。3 Issue は対象ファイルが重複せず依存も無かったため、
ユーザー承認を得て専用ブランチ・専用worktreeのまま並列実行した。

### 00:26 | src/lib/exp.ts
**操作**: 追加・修正
Issue #8。EXP計算式・主副配分・減点時フロアを実装した。画面からは呼ばない（Step 3 で GM が使う）が、
`02_architecture.md` §4.5 が AC-1 の型検査対象と定めているため実装を省略していない。
その後 `/code-review` の指摘で `completion` を `[0,1]` にクランプする処理を入口に追加した。
`completion` は `0..1` の想定だが未クランプで、GM やダミー値に `1.5` や負値が入ると
EXP が黙って増幅・負転するため。NaN は 0 に倒す。

### 00:26 | src/test/exp.test.ts
**操作**: 追加・修正
Issue #8 の境界値検証。`/code-review` で「テスト名が主張と逆」（`.5 未満なら切り下げ` と書きながら
`3.5 → 4` の切り上げを検証していた）と指摘され修正した。調べた結果、`SUB_STATUS_RATIO = 0.5` かつ
exp が整数である限り積の端数は `.0` か `.5` にしかならず、「切り下げ」ケースは原理的に発生しない。
そのため切り下げテストを足すのではなく、偶数/奇数の2ケースを丸め方向の retaining test として
明示する形にした（`Math.floor` に変えると奇数ケースが全て1ずれて落ちる）。

### 00:26 | src/lib/skill.ts
**操作**: 追加
Issue #9。《構造化》の Skill Lv 導出・構造化ボーナス・派生5種の解放判定を実装した。
`docs/TODO.md` の依存契約は「`level.ts` は型のみ」としていたが、`derivationStates` は
`StatusMap`（exp 保持）からステータス Lv を求める必要がある。`02_architecture.md` §6 L561 が
`L3 exp.ts skill.ts（level に依存。互いに知らない）` と定めているため、正典側に従って
`levelFromExp` を値として import した。`level.ts` からの逆参照は無く madge でも循環0件。

### 00:26 | src/test/skill.test.ts
**操作**: 追加
Issue #9 の境界値検証（発動回数 0/9/10/24/25/49/50/99/100/500/9999、派生の AND 条件、
TOTAL Lv9 でも派生未解放なら最終クラス未到達）。

### 00:27 | src/lib/data-source.ts
**操作**: 追加
Issue #10。静的JSONを読む唯一の入口。`phase` 分岐を1行に閉じ、`import "server-only"` で
クライアントからの import をビルド失敗にする。`server-only` を dependencies に追加した。

### 00:45 | src/test/data-source.test.ts
**操作**: 追加
`/code-review` と Codex セカンドレビューの双方が「JSON を `as` でキャストしているため
typecheck がデータ崩れを検出できない」と指摘したため追加した。JSON の widen された型が
キャスト先の supertype になるので型検査を素通りし、キー欠落やタイポは server component の
実行時 undefined 参照として初めて表面化する。`STATUS_ORDER` の9キー充足・HPレンジ・
quests の5キー・deadline の解釈可能性・history の levels を main/calibration 両方で検証する。
`server-only` は node 環境の vitest で例外を投げるため `vi.mock` で空モジュールに差し替えている。

### 00:38 | package.json
**操作**: 修正
Issue #1 の受け入れ条件・提供に含まれる `check:cycles` スクリプトが未実装のまま
Wave 1 のバリアを通過していたため補った（Issue #24 の受け入れ条件もこれに依存する）。
その後 Codex セカンドレビュー P1 で「`--ts-config` なしの madge は tsconfig の paths を
解決できず13ファイルを unresolved としてスキップしたまま成功終了する」と指摘され、
`--ts-config tsconfig.json` を追加した。循環ゼロの保証が偽の緑になっていたため。
修正前 Processed 20 / Skipped 13 → 修正後 Processed 26 / Skipped 1（外部の tailwindcss のみ）。
再現性のため `madge` を devDependencies に追加している。

### 00:47 | docs/04_exp_rules.md
**操作**: 修正
§7 例1 の副ステータス配分が `+58` と書かれていたが、同じ行が「50%換算・四捨五入」と
述べており内部矛盾していた（117 × 0.5 = 58.5 → 四捨五入なら 59）。
`02_architecture.md` §4.5 の「主100% / 副50%（四捨五入）」と Issue #8 の受け入れ条件も
四捨五入で一致しているため、実装ではなくこの例の数値を誤りと判断し `+59` に修正した。
ユーザー確認済み。
